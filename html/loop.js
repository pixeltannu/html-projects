let obj = {
    a: 1,
    b: 2,
    c: 3
}

// a.for (const key in obj) {
//  if (Object.hasOwn(obj, key)) {
  //      const element = obj[key];
        console.log(Key, element)
    }
}

    for (cons iterator of a) {
        console.log(iterator)
    }

//  Line                        Kya ho raha hai
//  for (const key in obj)      obj ke har key pe loop chala
//  obj.hasOwnProperty(key)     check karta hai key obj ki apni hai
//  obj[key]                    us key ki value nikali
//  console.log(element)        value print ki